export interface AppConfig {
  sampleHeight: number;
  longestSample: number;
  showIntro: boolean;
}

const appConfig = {
  sampleHeight: 40,
  longestSample: 5,
  showIntro: false,
};

export default appConfig;
